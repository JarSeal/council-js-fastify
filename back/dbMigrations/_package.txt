{
  "name": "council-fastify-migrate",
  "version": "1.0.0",
  "description": "Council migrations",
  "author": "JarSeal",
  "main": "index.ts",
  "license": "MIT",
  "private": false,
  "type": "module",
  "scripts": {
    "migrate": "migrate up --config-path ./",
    "migrate:up": "yarn migrate",
    "migrate:down": "migrate down --config-path ./",
    "migrate:create": "migrate create --config-path ./",
    "migrate:list": "migrate list --config-path ./",
    "migrate:prune": "migrate prune --config-path ./"
  },
  "devDependencies": {
    "ts-migrate-mongoose": "3.8.3"
  },
  "packageManager": "yarn@1.22.15",
  "engines": {
    "node": ">=16.15.0",
    "yarn": ">=1.22.15"
  }
}
