version: '3.5'

services:
  mongodb:
    image: mongo:4.4.10
    container_name: councildb
    environment:
      - PUID=1000
      - PGID=1000
    volumes:
      - ./database:/data/db
    ports:
      - 27027:27017
    restart: unless-stopped
