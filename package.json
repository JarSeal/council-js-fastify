{
  "name": "council-fastify",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/JarSeal/council-fastify.git",
  "author": "JarSeal",
  "license": "MIT",
  "private": false,
  "scripts": {
    "setup": "yarn && oneenv && cd back && yarn",
    "env": "oneenv",
    "dev": "yarn --cwd ./back/ dev",
    "build": "yarn --cwd ./back/ build",
    "lint": "yarn --cwd ./back/ lint",
    "test": "yarn --cwd ./back/ test",
    "startdb": "yarn --cwd ./back/ startdb",
    "stopdb": "yarn --cwd ./back/ stopdb",
    "devdb": "yarn --cwd ./back/ devdb"
  },
  "packageManager": "yarn@1.22.15",
  "engines": {
    "node": ">=16.15.0",
    "yarn": ">=1.22.15"
  },
  "devDependencies": {
    "oneenv": "0.9.9",
    "pino-pretty": "10.0.0"
  }
}
